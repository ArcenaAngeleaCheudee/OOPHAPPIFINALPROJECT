import tkinter as tk
from tkinter import messagebox

BACKGROUND_COLOR = "black"
WINDOW_BG = "#1e1e1e" 
TEXT_COLOR = "white"

BRIGHT_ACCENT = "#ffff66"  
ACCENT_TEXT = "black"      
HOVER_ACCENT = "#cccc55"   

SECONDARY_BUTTON_BG = "#555555" 
SECONDARY_HOVER = "#666666"

ENTRY_BG = "white"
FONT_FAMILY = ("Arial", 10) 
LARGE_FONT = ("Arial", 14, "bold")
TITLE_FONT = ("Arial", 24, "bold")

class HAPPI_LoginRegister(tk.Tk):
    def __init__(self):
        super().__init__()
        self.title("HAPPI: Login/Register Interface")
        self.geometry("800x600")
        self.configure(bg=BACKGROUND_COLOR)
        
        container = tk.Frame(self, bg=BACKGROUND_COLOR)
        container.pack(fill="both", expand=True)
        container.grid_rowconfigure(0, weight=1)
        container.grid_columnconfigure(0, weight=1)
        
        self.frames = {}

        for F in (StartingPage, RegisterPage, LoginPage):
            page_name = F.__name__
            frame = F(parent=container, controller=self)
            self.frames[page_name] = frame
            frame.grid(row=0, column=0, sticky="nsew")

        self.show_frame("StartingPage")
    
    def show_frame(self, page_name):
        frame = self.frames[page_name]
        frame.tkraise()


class BasePage(tk.Frame):
    def __init__(self, parent, controller):
        tk.Frame.__init__(self, parent, bg=BACKGROUND_COLOR)
        self.controller = controller

class StartingPage(BasePage):
    def __init__(self, parent, controller):
        super().__init__(parent, controller)
        
        tk.Label(self, text="HAPPI", 
                 font=("Arial", 48, "bold"), 
                 fg=TEXT_COLOR, bg=BACKGROUND_COLOR).pack(pady=(100, 10))

        subtitle_text = "HOLISTIC ACADEMIC AND PERSONAL\nPRODUCTIVITY INTERFACE"
        tk.Label(self, text=subtitle_text, 
                 font=FONT_FAMILY, 
                 fg=TEXT_COLOR, bg=BACKGROUND_COLOR).pack(pady=5)
        
        tk.Button(self, text="Register", 
                  font=LARGE_FONT, 
                  bg=BRIGHT_ACCENT, fg=ACCENT_TEXT, activebackground=HOVER_ACCENT, 
                  width=15, height=2,
                  command=lambda: controller.show_frame("RegisterPage
